#include <stdio.h>
#include <math.h>


//funtion prototype: it is a declaration of a function, but does not include the body of the function

// Standard library functions is a predefined function that has been defined in the standard library file (e.g math.h, stdio.h etc) and we can use it in our programs
// ctype.h is a character type  header file

int addNumbers(int number1, int numb;

int main(){
    int result = addNumbers(5, 2);
    printf("result = %d\n", result);

    return 0;
}

int addNumbers(int number1, int number2) {
    int sum = number1 + number2;
    return sum;
}

  

